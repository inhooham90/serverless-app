module.exports = {
    // what are we trying to compile down
    target: 'node',
    mode: 'production',
    // we can change the mode to none to see a better looking 
    // lambda code on the aws console.
    // mode: 'none',
}